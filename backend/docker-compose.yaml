version: "3.8"

services:
  statpitch-api:
    image: ghcr.io/nikp9/statpitch-api:latest
    container_name: statpitch-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - statpitch-net

networks:
  statpitch-net:
    external: true
